pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
        maven("https://maven.pkg.jetbrains.space/public/p/ktor/eap")
        maven("https://cache-redirector.jetbrains.com/maven-central")
    }
}

rootProject.name = "easy-query-plugin"